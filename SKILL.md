---
name: tweet-hook
description: >
  Research viral X/Twitter posts, reverse-engineer opening hooks (contradiction,
  anxiety, curiosity, controversy, heat), and generate punchy tweet openers.
  Use when the user wants tweet hooks, 推文开头, 爆款开头, hook formulas, X/Twitter
  copy patterns, or runs /tweet-hook.
metadata:
  short-description: "爆款推文开头：抓取分析 + 生成钩子"
---

# Tweet Hook Skill（爆款推文开头）

帮用户从 X 上高互动帖里提炼「开头钩子」写法，并批量生成可用开头。默认中文输出；用户用英文时跟用户语言。

## Capabilities & Limits

**能做：**
- 用 X 工具读公开推文、线程、用户时间线风格内容
- 按互动（赞/转/评）找爆款信号
- 拆解开头套路并生成新开头

**做不到 / 需用户配合：**
- 不能静默导出对方「关注列表」私有完整 dump；需要用户提供：
  - 账号 handle（如 `@someone`），和/或
  - 列表名 / 列表里的博主 handle 列表（粘贴或 `@a @b @c`）
- 私密账号、删帖、地区限制内容可能读不到
- 不要编造具体数据（粉丝数、浏览量）；抓不到就标明「未验证」

## When Invoked

1. 若用户没给博主列表：先问 **handle 列表** 或「从哪些账号学」。
2. 若只给了自己的账号：用搜索找其互动过/同圈层账号，或请用户粘贴关注列表里要分析的博主。
3. 确认语种、赛道（AI / Web3 / 成长 / 副业等）、目标（涨粉 / 转化 / 话题）。

## Research Workflow（抓取与拆解）

对每个目标账号（建议每轮 5–15 个，太多则分批）：

### A. 找爆款帖

优先用工具（按可用性）：

1. `x_keyword_search`  
   - `from:handle min_faves:N` 或 `from:handle min_retweets:N`  
   - `mode: Latest` 扫近期，`mode: Top` 看热门  
   - 中文内容可加关键词；N 按账号体量调（小号 50–200，中号 200–1000，大号更高）
2. `x_semantic_search`  
   - 语义找「焦虑/争议/反常识」类高相关帖  
   - 可用 `usernames: [...]` 收窄
3. `x_thread_fetch`  
   - 对长线程取全文，看钩子如何接正文
4. `x_user_search`  
   - 仅在需要确认账号身份时用

每账号尽量收集 **3–8 条** 高互动样本（有正文的原创帖优先，纯转发可降权）。

### B. 只分析「开头」

对每条样本截取 **前 1–2 句 / 前 40–80 字**（到第一个换行或问号/感叹号），记录：

| 字段 | 说明 |
|------|------|
| 账号 | @handle |
| 开头原文 | 原话 |
| 钩子类型 | 见下方类型表 |
| 机制 | 为什么让人停住（一句） |
| 互动信号 | 赞/转/评（有则记） |
| 可复用骨架 | 去掉具体名词后的句式 |

### C. 归纳（必须输出）

输出一节 **「开头规律总结」**，至少覆盖：

1. **制造矛盾** — 前后冲突、反常识、打脸旧信念  
2. **制造焦虑** — 错过/落后/代价/时间窗  
3. **制造话题** — 站队、命名现象、对立阵营  
4. **制造热度** — 数字、结果、名人/热点、极端案例  
5. **抛出钩子** — 开放循环、信息缺口、承诺揭晓  

每种给：定义一句话 + 从样本抽出的 **2–4 个句式模板** + **1 个改写示例**。

完整类型与句式库见：`references/hook-patterns.md`（生成前读一遍，按需引用，勿整文件贴给用户）。

### 已沉淀：用户 List 研究库（优先读）

若用户提到 **该 List**、list id `2067293665865998356`、或「按我列表里的博主风格写开头」：

1. **必读** `references/list-2067293665865998356/README.md`（范围与限制）
2. **必读** `references/list-2067293665865998356/swipe-openers.md`（逐人开头 + 骨架 + 跨账号规律）
3. 官方号对照：`references/list-2067293665865998356/official-pr.md`（少学）
4. 生成时优先套用 swipe 里的 **骨架与组合表**，不要只靠通用 hook-patterns
5. 若用户要求 refresh：用 `list:2067293665865998356` + 各 `from:handle min_faves:N` 补采，合并进 swipe-openers.md

**说明：** 完整 List 成员无法 API 导出；库内为高活跃个人博主子集（约 9 人 × 约 8–10 条高赞开头）。用户可贴更多 handle 扩展。

## Generation Workflow（帮人写开头）

用户给出主题 / 产品 / 观点后：

1. 若适用 List 库：先按目标（涨粉/收藏/认知/转化）选 swipe 中的「默认组合」。
2. 选 2–3 个最适合的钩子类型（不要五种硬凑）。
3. 每种生成 **3–5 条** 可直接粘贴的开头（短、狠、可扫读）；可标注「像 @xiaohu / 像 @dontbesilent」。
4. 每条标注：`类型 | 风险（低/中/高） | 适合（涨粉/转化/讨论）`。
5. 再给 **1 条「安全版」+ 1 条「进攻版」** 对照。
6. 可选：补 1 段「开头之后第一句怎么接」，避免标题党断崖。

### 写作约束

- 一行内建立冲突或缺口；少形容词，多具体（数字、对比、时间、身份）。
- 避免违法、医疗金融绝对承诺、煽动仇恨；焦虑类用「揭示代价」而非恐吓欺诈。
- 不假装已验证的虚假数据；需要数字时用用户提供的或写「据我观察/我们测下来」。
- 中文推文开头默认 **15–40 字** 更易完读；需要英文时 **8–16 words** 量级。
- 可给「可复制骨架」：`{身份} {反常识结果}，但 {多数人做法}。`

## Output Templates

### 研究模式

```markdown
## 样本池
- @a / @b / ...（N 条）

## 开头拆解表
| 账号 | 开头 | 类型 | 机制 | 骨架 |

## 五类规律
### 矛盾
### 焦虑
### 话题
### 热度
### 钩子

## 你的可复用句式（Top 10）
1. ...
```

### 生成模式

```markdown
## 主题
...

## 推荐开头
1. ...
2. ...

## 骨架
- ...

## 不建议
- ...（说明原因）
```

## Quality Bar

- 开头必须 **能单独停滑**；读完不知道「要不要点开」= 失败。
- 优先具体 > 鸡汤；对比 > 形容；结果 > 过程口号。
- 同一批开头避免重复同一句式超过 2 次。

## Optional: Build a personal swipe file

若用户要求沉淀：把高频骨架追加写入  
`~/.grok/skills/tweet-hook/references/swipe-file.md`  
（仅骨架与类型，不存他人全文长文，注意版权与平台规则）。
