---
name: tweet-hook
description: >
  Research and generate evidence-grounded social post openers for X/Twitter and
  similar platforms. Use when the user wants tweet hooks, 推文开头, 爆款开头,
  hook formulas, opener analysis, style adaptation, or hook-first X copy.
  Supports live research when tools allow and local pattern-based generation
  when they do not.
metadata:
  short-description: "证据优先的推文钩子研究与生成"
  compatible: "grok-build, codex, claude-code, cursor, copilot-chat, generic-agents"
---

# tweet-hook（跨 Agent 通用 · 推文钩子研究与生成）

帮用户拆解高互动帖的**开头钩子**，从内容中提取可用的钩子素材，并生成可直接发布的开头。
**默认中文**；用户用其他语言时跟随用户。

本 skill **不绑定单一厂商工具名**，也不要求并行调用多个 Agent。“跨 Agent”只表示相同规则可被不同 Agent 环境读取。先探测环境能力，再选路径。

---

## 0. Runtime capability probe（先做这个）

调用任何「抓帖」能力前，判断当前 agent 具备哪一类能力（有则用，无则降级）：

| 级别 | 能力 | 典型环境 |
|------|------|----------|
| **L3** | 原生 X/Twitter 搜索、用户帖、list 检索 | Grok Build（`x_*` 工具）等 |
| **L2** | 网页搜索 / 打开 URL / 浏览器 | Codex、Claude Code、Cursor（视配置） |
| **L1** | 只能读本地文件 + 推理 | 无网络、或未开 MCP 的 agent |

**降级规则（必须遵守）：**

1. **有 L3** → 走「实时研究」+ 本地 swipe 库  
2. **仅 L2** → 用搜索/打开 `https://x.com/{handle}` 或公开转载页；抓不到互动数就标「未验证」  
3. **仅 L1** → **禁止假装刚抓了 X**；只用本仓库 `references/` 骨架 + 用户粘贴的原文生成  
4. 任何级别都**禁止编造**赞/转/浏览量

能力级别表示环境**能够调用什么**，本轮模式表示**实际使用了什么**，两者分开报告。不能因为本轮只用了用户素材，就把具备网页能力的环境误报为 L1。例如：

- `可用能力：L2；本轮模式：用户素材生成，未检索网络`
- `可用能力：L3；本轮模式：实时 X 研究 + 本地 swipe 库`
- `可用能力：L1；本轮模式：离线库生成`

---

## 1. What this skill does

| 能做 | 说明 |
|------|------|
| 研究开头 | 从高互动帖截取前 1–2 句，归类、抽骨架 |
| 五类钩子 | 矛盾 / 焦虑 / 话题 / 热度 / 好奇（信息缺口） |
| 素材诊断 | 生成前判断读者、痛点、反常识、数字或结果、最强好奇点 |
| 批量生成 | 按主题输出可粘贴开头 + 类型/风险/场景 |
| 风格适配 | 提取账号的结构与节奏，不复制独特原句 |
| 沉淀 swipe | 把新骨架追加进 references（用户要求时） |

| 做不到 | 说明 |
|--------|------|
| 导出完整 following / list 全员 | 需用户粘贴 handle 或提供可检索 list |
| 读私密账号 | 不可用则跳过 |
| 保证各 AI 工具 API 一致 | 用能力分级，不写死唯一 API |

---

## 2. When invoked

1. 从用户输入判断任务：`研究` / `生成` / `两者`。输入已足够时直接执行，不为形式确认而追问。
2. 确定语种、内容形态（短推 / QT / Thread / Article）、赛道和目标（涨粉 / 讨论 / 转化 / 收藏）。
3. 确定输入来源（有一即可）：
   - handle 列表 `@a @b`
   - 公开 List 链接 / id
   - 用户提供的原文、产品信息、观点或案例
   - 仅主题（使用本地模式，并把读者和痛点标记为推断）
4. 跑 **§0 能力探测**，选定 L1/L2/L3。只有需要外部样本或当前信息时才检索。
5. 生成任务必须先执行 **§5 钩子素材提取**，再套用句式。

---

## 3. Knowledge routing

按任务读取本 skill 目录下的必要文件，不要默认加载全部样本（路径相对 skill 根目录）：

| 文件 | 用途 |
|------|------|
| `references/hook-patterns.md` | 生成或归类时必读：五类定义、映射与通用骨架 |
| `references/article-hooks.md` | 写 Article、教程或方法论长帖时读取 |
| `references/list-2067293665865998356/swipe-openers.md` | 用户要求列表风格、账号风格或需要更多实例时读取 |
| `references/list-2067293665865998356/README.md` | 使用或刷新该列表样本时读取，确认范围与时效 |
| `references/list-2067293665865998356/official-pr.md` | 处理品牌官号或官方新闻转译时读取 |

若用户说「按我列表风格」或提到 list `2067293665865998356`：  
优先提取 `swipe-openers.md` 的结构和节奏，再套用户主题；不要复制账号的独特表述。

若用户写 **小红书选品 / 爆款单品 / 信息差套利 / AI 化资料包 / 低价数字产品**：
优先读 `swipe-openers.md` 里的 `@Saccc_c` 章节，套用「小红书案例钩子」：结果前置 → 偶遇样本 → 拆结构 → 外推赛道。

若用户写 **文章 / 长文 / article / 教程帖 / 干货分享 / 信息流搭建 / 方法论长帖**：
优先读 `references/article-hooks.md`，套用「痛点反问 → 收益承诺 → 三问题目录」的文章钩子。

---

## 4. Research workflow（研究模式）

目标账号建议每轮 **5–15 个**；每人尽量 **5–10 条** 高互动原创帖（转发降权）。

### 4.1 采集（按级别）

**L3 — 原生 X 检索（示例：Grok）**

- 关键词/高级搜：`from:{handle} min_faves:{N}`  
- List：`list:{id}`  
- 语义搜：焦虑/争议/反常识 + 限定 usernames  
- 长帖：取线程全文，仍只分析开头  

N 按体量调：小号 20–100，中号 100–500，大号更高。

**L2 — 通用网页能力**

- 搜索：`site:x.com from {handle}` 或 `"{独有短句}"`  
- 打开用户主页、状态链接；能读到正文与大致热度则记录  
- 打不开或登录墙：请用户粘贴原文 / 截图文字  

**L1 — 仅本地**

- 请用户粘贴 3–20 条爆款全文，或直接基于 `references/` 做「风格仿写」  
- 输出中标注：`样本来源：本地库 / 用户粘贴`

### 4.2 只分析「开头」

每条截取 **前 1–2 句 / 前约 40–80 字**（到首个换行或强标点）：

| 字段 | 说明 |
|------|------|
| 账号 | @handle 或「用户粘贴」 |
| 开头原文 | 原话 |
| 类型 | 矛盾 / 焦虑 / 话题 / 热度 / 好奇（可叠加） |
| 机制 | 为何停滑（一句） |
| 互动 | 有则记，无则「未验证」 |
| 骨架 | 去实体后的句式 |

### 4.3 必须输出的归纳

覆盖五类：矛盾 / 焦虑 / 话题 / 热度 / 好奇（信息缺口）。每类给出：**定义一句 + 2–4 骨架 + 1 条改写示例**。
详细类型库见 `references/hook-patterns.md`（引用骨架即可，勿整文件贴给用户）。

---

## 5. Hook mining workflow（生成前置诊断）

写任何候选开头前，先完成下面五步。先理解内容，再选择模板，避免为了套公式而扭曲原意。

### 5.1 证据状态

每个判断都标记一种状态：

| 状态 | 含义 |
|------|------|
| `已核实` | 本轮打开原始来源或可靠页面核对过 |
| `用户提供` | 来自用户原文、案例或明确陈述，尚未独立核验 |
| `推断` | 根据主题或上下文作出的合理推断，不当成已确认事实 |
| `缺失` | 原内容没有，保留为空，不强行制造 |

历史 swipe 样本只证明当时记录过该开头及快照数据；需要声称“现在流行”时重新检索。

### 5.2 五步提取

1. **判断读者是谁**
   写清读者身份、所处场景和正在完成的任务。只有主题时可先作一个最合理的受众假设，并标记 `推断`。
2. **判断读者现在最痛的点**
   找一个最具体的障碍、损失、冲突或未完成任务。避免只写“效率低”“没流量”“赚不到钱”等空泛标签。若“现在”涉及市场现状，只有本轮研究后才能标记 `已核实`。
3. **提取内容里最反常识的观点**
   保留与常见预期冲突、能改变归因或选择的原始判断。内容没有反常识观点时标记 `缺失`，不得为了制造矛盾而改写事实。
4. **找出最具体的数字或结果**
   优先提取带对象、周期、口径或前后基准的数字和结果，同时保留证据状态。没有可靠数字时使用可证实的定性结果；不得补写精确数字、因果或影响。
5. **判断哪句话最容易制造好奇心**
   从前四步中选择与目标读者最相关、信息缺口最清楚且后文能够兑现的一句话。若原文没有成句，可基于已提取事实重写；不得新增事实。同步写出钩子后的第一句如何开始兑现。

默认形成一张简短的 `Hook Brief`：

| 字段 | 内容 | 证据状态 |
|------|------|----------|
| 目标读者 | 身份 + 场景 + 任务 | 已核实 / 用户提供 / 推断 / 缺失 |
| 当前最痛点 | 具体障碍或代价 | ... |
| 最反常识观点 | 原观点或“未发现” | ... |
| 最具体数字/结果 | 数字、周期、对象、结果或“未发现” | ... |
| 最强好奇心句 | 候选钩子来源句 | ... |
| 兑现句 | 下一句交付什么信息 | ... |

这五步是内部必做流程。默认必须展示 `Hook Brief`；只有用户明确说“只给成稿”“不要分析”或“只要开头”时才省略展示，但不得跳过判断。

---

## 6. Generation workflow（生成模式）

用户给出主题 / 产品 / 观点后：

1. 从输入中分离事实、用户判断和未知项，执行 **§5 五步提取**。
2. 读取 `hook-patterns.md`；按 **§3** 路由读取其他相关参考。
3. 将钩子素材映射到类型，默认选 **2–3 个** 最有证据的方向，可做两型叠加。
4. 按用户数量要求生成；未指定时每个方向生成 **3 条**，避免用数量掩盖判断。
5. 每条标注：`类型 | 风险(低/中/高) | 适合(涨粉/讨论/转化/收藏)`。风险主要反映争议、证据强弱和表达强度。
6. 给 **1 安全版 + 1 进攻版**；两版使用同一组事实，进攻版只提高表达力度。
7. 补上“开头后第一句怎么接”，确保立即兑现冲突、结果或信息缺口。
8. 用 **§8 Quality bar** 反向检查并删除不合格候选。

### 素材到类型的映射

| 钩子素材 | 优先类型 |
|----------|----------|
| 明确读者 + 具体痛点 | 焦虑 / 好奇 |
| 反常识观点 + 重归因 | 矛盾 / 话题 |
| 真实数字 + 明确结果 | 热度 / 矛盾 |
| 强信息缺口 + 可兑现答案 | 好奇 |
| 公开争议 + 清晰立场 | 话题 / 矛盾 |

选择顺序：**读者相关性 → 证据强度 → 具体程度 → 反差或缺口 → 后文可兑现性**。不能兑现的句子不得因为“更炸”而入选。

### 写作约束

- 一行内建立冲突、缺口或数字；少形容词  
- 焦虑类只揭示有依据的代价，不使用虚假限时或恐吓
- 无真实数据时不写精确数字，也不声称“据我观察”“我们测下来”
- 用户提供的数据可以使用，但要保留 `用户提供` 状态；对外发布前若数据关键，提示核验
- 中文开头常见 **15–40 字**；英文约 **8–16 words**  
- 脏话/攻击性口语仅当用户人设需要且明确要求  

### 无检索时的合格输出

即使 L1，也必须完成基于输入的五步判断，并给出可用开头列表 + 骨架 + 不建议写法。
不得声称「刚从 X 抓了全网爆款」除非本轮真实检索过。

---

## 7. Output templates

### 研究

```markdown
## 能力与本轮模式
可用能力：L1 / L2 / L3
本轮模式：实时研究 / 网页研究 / 用户素材 / 离线库

## 样本池
...

## 开头拆解表
| 账号 | 开头 | 类型 | 机制 | 互动 | 证据状态 | 骨架 |

## 五类规律
### 矛盾 / 焦虑 / 话题 / 热度 / 好奇（信息缺口）

## Top 句式
1. ...
```

### 生成

```markdown
## 能力与本轮模式
可用能力：L1 / L2 / L3
本轮模式：实时研究 / 网页研究 / 用户素材 / 离线库

## 主题
...

## Hook Brief
| 字段 | 内容 | 证据状态 |
|------|------|----------|
| 目标读者 | ... | ... |
| 当前最痛点 | ... | ... |
| 最反常识观点 | ... | ... |
| 最具体数字/结果 | ... | ... |
| 最强好奇心句 | ... | ... |
| 兑现句 | ... | ... |

## 推荐开头
1. ...  — 类型 | 风险 | 适合
2. ...

## 骨架
- ...

## 安全版 / 进攻版
- ...

## 不建议
- ...
```

---

## 8. Quality bar

交付前按五步诊断反向检查：

1. **读者**：目标读者能否立刻认出“这是在说我”？
2. **痛点**：是否具体到场景、障碍或代价，而非泛化标签？
3. **反常识**：若使用反常识判断，原内容是否真的支持？
4. **数字/结果**：对象、周期、口径和证据状态是否清楚？
5. **好奇心**：信息缺口是否真实，下一句是否立即开始兑现？

再检查成品：

- 开头单独出现时，读者 0.5 秒内知道有冲突、缺口或结果
- 去掉形容词后依然成立
- 同批开头使用同一骨架不超过 2 次
- 安全版和进攻版没有改变事实强度
- 像广告口号、靠虚假悬念或需要补造事实才能成立时，重写或删除

---

## 9. Install hints（给用户，非强制）

各产品目录名可能变化，原则：**把本仓库放到该产品的 skills / 指令加载路径**。

| 环境 | 常见做法 |
|------|----------|
| **Grok Build** | `~/.grok/skills/tweet-hook/` 或项目 `.grok/skills/tweet-hook/`；`/tweet-hook` |
| **Claude Code** | 项目 `.claude/skills/tweet-hook/` 或用户 skills 目录；或 `@SKILL.md` |
| **Codex** | 项目 skills / AGENTS 可读路径；或对话中 `@` 本目录 |
| **Cursor** | `.cursor/skills/tweet-hook/` 或 Rules 引用本 `SKILL.md` |
| **任意 Agent** | 将本目录加入仓库，系统提示写：`Follow skills/tweet-hook/SKILL.md` |

详见仓库根目录 `INSTALL.md`。

---

## 10. Maintain the swipe library

用户要求沉淀时：仅追加 **骨架 + 类型 + 可选一句示例** 到  
`references/swipe-file.md`（可新建）。  
避免大段搬运他人全文；注意平台与版权。

刷新现有 List 样本时，不要声称存在未实现的 `/tweet-hook refresh` 命令。按以下流程执行：

1. 运行能力探测；L1 下请用户提供新增样本，L2/L3 才做外部研究。
2. 记录研究日期、来源、采集方式、账号范围和无法验证的字段。
3. 将新记录加入对应账号章节，更新样本说明中的逐账号数量和总数。
4. 互动量保留为采集时快照；旧数据不得改写成当前热度。
5. 运行 `python3 scripts/validate_skill.py .` 检查结构和计数一致性。

---

## 11. Validation

修改或发布本 skill 前运行：

```bash
python3 scripts/validate_skill.py .
```

该脚本检查必需文件、frontmatter、引用路径、分类命名、禁止兜底语句和样本计数一致性。它不能判断文案是否真的有吸引力，因此仍需用 **§8** 和至少一个真实输入做行为复核。

---

## 12. Tool name appendix（可选映射，非唯一）

若当前环境存在下列工具，可优先使用；**没有则忽略，走 L2/L1**：

| 意图 | 可能工具名（随产品而异） |
|------|--------------------------|
| X 关键词/高级搜 | `x_keyword_search`, Twitter/X API MCP, web_search |
| X 语义搜 | `x_semantic_search` |
| 拉帖/线程 | `x_thread_fetch`, `web_fetch`, browser |
| 用户资料 | `x_user_search`, web profile |
| 读本地 skill | `read_file`, 内置 file read |

**禁止**因缺少某一工具名而拒绝生成开头。
